#include <iostream>
#include <algorithm>

using namespace std;

int x[10005];
int y[10005];
int p[10005];
int l[10005];
int h[10005];
int f[10005][1005];

int main()
{
	ios::sync_with_stdio(false);
	int n,m,k;
	cin >> n >> m >>k;
	for (int i = 0; i < n; ++i)
	 cin >> x[i] >> y[i];
	for (int i = 1; i <= k; ++i)
	 cin >> p[i] >> l[i] >> h[i];
	for (int i = 1; i <= n; ++i)
	{
		for (int j = 1; j <= m; ++j)
		{
			
		}
	}
	return 0;
} 
