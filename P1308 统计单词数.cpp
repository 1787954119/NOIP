#include <iostream>
#include <cstring>

using namespace std;

char a[1000005];
char b[15];

int main()
{
	ios::sync_with_stdio(false);
	int x = 0,y = 0;
	int count = 0;
	int j = 0
	cin >> b >> a;
	x = strlen(a);
	y = strlen(b);
	for (int i = 0; i < x; ++i)
	{
		++j;
		if (j == y)
		{
			
		}
	}
	return 0;
}
